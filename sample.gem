Event battleEvent;
Battle b1;

void init(int a, int b) {
	Hero Daniel = new Hero(1,2,3,4,5);

	Monster dragon = new Monster(1,2,3,4);

	b1 = new Battle(a,b);

	battleEvent = new Event(a,b) {
		int res = 0;
		if (1 == 1)
			res = 1;
		next res;
	};
}

void main() {
	init();
	Event story = battleEvent;
}