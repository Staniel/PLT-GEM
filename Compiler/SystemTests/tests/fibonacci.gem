int fib(int number) {
	if (number == 1 || number == 2) {
		return 1;
	}
	return fib(number-1) + fib(number-2);
}

void main(String[] args) {
	int number = 15;
	int result = fib(number);
	print result;
}
	
	
